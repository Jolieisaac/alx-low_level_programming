#include "main.h"

/**
* main - prints the numbers, from 0 to 9, followed by a new line
*
* Return: Always 0.
*/

void print_numbers(void);

{

	char c;
	for (c = 0; c < 10; c++)
	{
	_putchar(c);
	}
	printf("\n");
}
